import React from 'react';
import { hot } from 'react-hot-loader';
import CategoryFolderWidget from './containers/CategoryFolderWidget';

const Widget = () => (
  <CategoryFolderWidget />
);

export default hot(module)(Widget);
